<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ink.dao.FontDao">

    <!-- Result maps describe the mapping between the columns returned
   from a query, and the class properties.  A result map isn't
   necessary if the columns (or aliases) match to the properties
   exactly. -->
    <resultMap id="FontResult" type="com.ink.entity.Font">
        <result property="id" column="id"/>
        <result property="font_color" column="font_color"/>
        <result property="font_size" column="font_size"/>
        <result property="border_color" column="border_color"/>
        <result property="border_size" column="border_size"/>
    </resultMap>
</mapper>